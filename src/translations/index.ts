import { resources } from "./resources";
import languages from "./resources";

export const config = {
  lng: "en",
  fallbackLng: "en",
  interpolation: { escapeValue: false },
  resources,
};

export { languages };
